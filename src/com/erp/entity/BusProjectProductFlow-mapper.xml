<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="busprojectproductflow">
    <sql id="selectId">
        flowId,
        productNo,
        productName,
        status,
        bak
    </sql>

    <select id="query" resultType="BusProjectProductFlow" parameterType="java.util.HashMap">

        select
        <include refid="selectId" />
        from BusProjectProductFlow  where flowId = #{flowId}
    </select>

    <!-- total count -->
    <select id="count" parameterType="BusProjectProductFlow" resultType="int">
        select count(0) from BusProjectProductFlow
    </select>

     <select id="getByFlowId" parameterType="String" resultType="BusProjectProductFlow">
        select * from BusProjectProductFlow where flowId=${flowid}
    </select>
    
    <select id="conditionSelect" parameterType="BusProjectProduct" resultType="int">
        select count(*) from BusProjectProductFlow where productNo = #{productNo} and productName= #{productName} and status= #{status} and flowId 
        in (select flowid from busprojectproductprojectflow where projectId=#{projectId})
    </select>
    <insert id="add" parameterType="BusProjectProductFlow">
        insert into BusProjectProductFlow(
        flowId,
        productNo,
        productName,
        status,
        bak )
        values (
        #{flowId},
        #{productNo},
        #{productName},
        #{status},
        #{bak})
    </insert>
    <update id="update" parameterType="BusProjectProductFlowVo">
        UPDATE BusProjectProductFlow
        <set>
          productNo = #{productNo},
          productName = #{productName},
          status= #{status},
          bak= #{bak}
        </set>
         <where>
            status=#{statusOld} and productNo=#{productNoOld} and productName=#{productNameOld} and flowId=#{flowId}
         </where>
    </update>
    <delete id="deleteById" parameterType="BusProjectProductFlow">
        delete from BusProjectProductFlow where productNo=#{productNo} and productName=#{productName} and flowId=#{flowId}
    </delete>
</mapper>
